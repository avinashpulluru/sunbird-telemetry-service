apiVersion: v1
kind: Service
metadata:
    name: avinashpullurusunbirdtelemetryservice
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: avinashpullurusunbirdtelemetryservice